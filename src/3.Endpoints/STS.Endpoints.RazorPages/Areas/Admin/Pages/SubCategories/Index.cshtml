@page
@model STS.Endpoints.RazorPages.Areas.Admin.Pages.SubCategories.IndexModel
@{
    ViewData["Title"] = "استارسرویس > مدیریت زیر دسته بندی ها > لیست زیر دسته بندی ها";
}

<div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>

<div class="col-lg-12">
    <div class="portlet box shadow">
        <div class="portlet-heading">
            <div class="portlet-title">
                <h3 class="title">
                    زیر دسته بندی ها
                </h3>
            </div><!-- /.portlet-title -->
        </div><!-- /.portlet-heading -->
        <div class="portlet-body">
            <div class="top-buttons-box mb-2">
                <a class="btn btn-success" asp-page="Create">
                    <i class="icon-plus"></i>
                    افزودن
                    <div class="paper-ripple"><div class="paper-ripple__background"></div><div class="paper-ripple__waves"></div></div>
                </a>
            </div>
            <div class="row">
                <div class="table-responsive">
                    <table class="table table-bordered table-hover table-striped" id="data-table-SubCategory">
                        <thead>
                            <tr>
                                <th>شناسه</th>
                                <th>نام دسته بندی</th>
                                <th>عملیات</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var subcategory in @Model.SubCategories)
                            {
                                <tr>
                                    <td>@subcategory.Id</td>
                                    <td>@subcategory.Name</td>
                                    <td>
                                        <a asp-page="Update" asp-route-Id="@subcategory.Id" class="btn btn-info">ویرایش</a>
                                        <a onclick="return confirm('آیا از حذف این زیر دسته یندی اطمینان دارید؟')" asp-page-handler="Delete" asp-route-Id="@subcategory.Id" class="btn btn-danger">حذف</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div><!-- /.portlet-body -->
    </div><!-- /.portlet -->
</div><!-- /.col-lg-12 -->
@section Scripts
{
    <script src="~/AdminTemplate/plugins/data-table/js/jquery.dataTables.min.js"></script>
    <script src="~/AdminTemplate/js/pages/datatable.js"></script>
}